#Find the number with the most digits.

#If two numbers in the argument array have the same number of digits, return the first one in the array.


def find_longest(arr):
    longest_num = 0
    for i in arr:
        if len(str(i)) > len(str(longest_num)):
            longest_num = i
    return longest_num
