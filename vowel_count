#Return the number (count) of vowels in the given string.

#We will consider a, e, i, o, and u as vowels for this Kata.


#first solution

def getCount(inputStr):
    num_vowels = 0
    for ltr in inputStr:
        if ltr == 'a' or ltr == 'e' or ltr == 'i' or ltr == 'o' or ltr == 'u':
            num_vowels += 1
    return num_vowels


#second solution. optimized, but not optimal

def getCount(inputStr):
    num_vowels = 0
    for ltr in inputStr:
        if ltr in 'aeiouAEIOU':
            num_vowels += 1
    return num_vowels
    
    
